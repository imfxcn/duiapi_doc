---
title: "Requests"
description: "Send requests to DUI API"
---

Requests use standard HTTP methods, headers, query parameters, and JSON bodies.

Use `GET` to read data, `POST` to create resources or trigger workflows, and `PATCH` to update supported resources.

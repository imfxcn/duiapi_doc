---
title: "Webhooks"
description: "Receive events from DUI API"
---

Use webhooks to receive event notifications when resources change or workflows complete.

Configure an endpoint that can receive HTTPS `POST` requests.
